import { InferGetServerSidePropsType } from 'next';
import Heading from '@components/Heading/Heading';
import Text from '@components/Text/Text';
import Page from '@layouts/Page';
import { baseGetServerSideHandler } from '@util/getProps';

export const getServerSideProps = baseGetServerSideHandler(
  {
    translationNamespaces: ['common'],
    meta: {
      title: '{{startCase this.name}}',
      description: 'Description',
    },
  },
  async () => {
    return {
      props: {
        desc: 'Description',
      },
    };
  },
);
type Props = InferGetServerSidePropsType<typeof getServerSideProps>;

const {{pascalCase this.name}} = ({ meta, data }: Props) => {
  return (
    <Page meta={meta}>
      <Heading as="h1">{{this.name}}</Heading>
      <Text size="large">{data.desc}</Text>
    </Page>
  );
};

export default {{pascalCase this.name}};
